---
import Section from "../../components/Section/Section.astro";
import Sentences from "../../components/Sentence/Sentences.tsx";
import Layout from "../../layouts/Layout.astro";
import {
  firstFormExamples,
  genericExamples,
  notAloneExamples,
  secondFormExamples,
  secondFormExamplesStartWithSentence,
} from "../../utils/examples/hovedsætning";
import { COLORS } from "../../utils/fixedColors.ts";
---

<Layout
  title={{
    text: "Main clauses",
    tooltip: "Hovedsætninger",
  }}
>
  <Section title="A sentence that can stand alone and make sense.">
    <Sentences client:only="react" sentences={genericExamples} />
  </Section>

  <Section title="1st form:">
    <h3>
      <span class={COLORS.Subject.class}>Subject</span>
      <span class={COLORS.Verb.class}>Verb</span>
      <span class={COLORS.Adverb.class}>(Adverb)</span> -
      <span class={COLORS.Object.class}>Object</span>
    </h3>
    <p>Used when the sentence starts with the subject.</p>
    <Sentences client:only="react" sentences={firstFormExamples} />
  </Section>

  <Section title="2nd form (inversion):">
    <h3>
      <span class={COLORS.Verb.class}>Verb</span>
      <span class={COLORS.Subject.class}>Subject</span>
      <span class={COLORS.Adverb.class}>(Adverb)</span> -
      <span class={COLORS.Object.class}>Object</span>
    </h3>
    <p>Used when the sentence does not start with the subject.</p>
    <Sentences client:only="react" sentences={secondFormExamples} />
    <p>
      Also used when a sentence starts with "another sentence"<br />and it is a
      main clause (it can stand alone and make sense)
    </p>
    <Sentences
      client:only="react"
      sentences={secondFormExamplesStartWithSentence}
    />
    <div class="divider mb-10"></div>

    <h3>
      A main clause doesn't <span class="italic">need</span> to stand alone. It needs
      to be <span class="italic">able</span> to stand alone.
    </h3>
    <p>More sentences could follow or precede.</p>

    <Sentences client:only="react" sentences={notAloneExamples} />
  </Section>
</Layout>
